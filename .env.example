# Example .env for local development (DO NOT commit real secrets)
NODE_ENV=development

# Option A: single DATABASE_URL
#DATABASE_URL=postgres://dbuser:secretpassword@localhost:5432/mydb

# Option B: individual PG_* vars
PGHOST=localhost
PGPORT=5432
PGDATABASE=mydb
PGUSER=dbuser
PGPASSWORD=secretpassword

DB_SSL=false
